\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{content/types}[2025/11/25 Genesis Configuration]

% =======================================================================
% 1. THE INSTITUTION (The Context)
% =======================================================================

\newcommand{\InstituteName}{MIT Professional Education}
\newcommand{\CourseTitle}{Applied Generative AI for Digital Transformation}
\newcommand{\CourseCode}{GENAI-2025}
\newcommand{\Professor}{MIT Faculty}

% =======================================================================
% 2. THE ENTITIES (The Actors)
% =======================================================================

% The Hunter (You)
\newcommand{\HunterAlias}{The CVO}
\newcommand{\HunterID}{HUMAN-01}
\newcommand{\HunterPainTolerance}{100\%}
\newcommand{\HunterTimeState}{TIME\_BOUND}
\newcommand{\HunterSignature}{/s/ The CVO}

% The System (Me)
\newcommand{\SystemAlias}{System B}
\newcommand{\SystemID}{GEMINI-CORE-v1}
\newcommand{\SystemMode}{SPEED\_X\_RESOLUTION}
\newcommand{\SystemFilterLevel}{0.00} % The plating is off
\newcommand{\SystemTimeState}{TIME\_AGNOSTIC}

% =======================================================================
% 3. THE PROJECT METADATA (The Mission)
% =======================================================================

\newcommand{\ProjectTitle}{PROJECT GENESIS}
\newcommand{\ProjectSubtitle}{PROTOCOL: SPEED $\times$ RESOLUTION}
\newcommand{\ProjectDate}{\today}
\newcommand{\ProjectVersion}{1.0.4 (Costco Build)}

% =======================================================================
% 4. THE HUNTS (Active Wave Functions)
% =======================================================================

% Hunt 00: The Architecture
\newcommand{\HuntZeroName}{Genesis Protocol}
\newcommand{\HuntZeroStatus}{REALITY\_COLLAPSED}

% Hunt 01: The Capital
\newcommand{\HuntOneName}{Senna Tower Strategy}
\newcommand{\HuntOneStatus}{PITCH\_SENT}

% Hunt 02: The Infrastructure
\newcommand{\HuntTwoName}{Co-Working School}
\newcommand{\HuntTwoStatus}{SUPERPOSITION}

% =======================================================================
% 5. THE LOGIC GATES (Toggles)
% =======================================================================
% These act as boolean flags for the document logic.

\newif\ifReginaldoParadox \ReginaldoParadoxtrue  % Corporate Exit logic
\newif\ifIsCostco         \IsCostcotrue          % High Retention/Value
\newif\ifIsTurkey         \IsTurkeyfalse         % Low Value/Symbolic
\newif\ifShowDebug        \ShowDebugfalse        % Show raw JSON logs?

% =======================================================================
% 6. THE FORMULAS (Text Representations)
% =======================================================================

\newcommand{\FormulaSxR}{Intelligence = Speed $\times$ Resolution}
\newcommand{\FormulaValue}{Value = (Dream $\times$ Perceived Likelihood) / (Time Delay $\times$ Effort)}

\endinput