\documentclass{article}

% 1. GLOBAL CONFIG
\usepackage[margin=0cm, paperwidth=8.5in, paperheight=11in]{geometry}
\usepackage{xcolor}
\usepackage{ocgx2}
\usepackage{hyperref}
\usepackage{tikz}
\usetikzlibrary{backgrounds, fit}

% 2. CONFIGURATION (Load First)
\usepackage{content/types}

% 3. MODULE IMPORTS
\usepackage{styles/colors}
\usepackage{styles/typography}
\usepackage{styles/layout}
\usepackage{styles/notation}

% 4. COMPONENTS
\usepackage{components/bubbles}
\usepackage{components/terminal}
\usepackage{components/manifest}

% 5. BIBLIOGRAPHY (Load before document)
\usepackage[style=numeric, backend=biber, sorting=none]{biblatex}
\addbibresource{references.bib}

% 6. METADATA
\title{PROJECT GENESIS}
\author{The CVO}
\date{\today}

% 7. RENDER SEQUENCE
\begin{document}
    \begin{Form}
        % Front Matter (Cover, Abstract, TOC)
        \input{content/frontmatter}
        
        % Main Content (The Payload)
        \input{content/payload}
        
        % Appendices (Single instance)
        \newpage
        \appendix
        \input{content/appendices/appendix_a}
        \newpage
        \input{content/appendices/appendix_b}
        
    \end{Form}
\end{document}